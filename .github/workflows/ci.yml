name: CI

on: [push]

jobs:
  build-test:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout repositório
        uses: actions/checkout@v3

      - name: Instalar dependências
        run: npm install

      - name: Rodar Cypress
        uses: cypress-io/github-action@v5
        with:
          start: npm run dev
          build: npm run build
          wait-on: 'https://opensource-demo.orangehrmlive.com/web/index.php'
          wait-on-timeout: 60

      - name: Upload de artefatos (Cypress)
        uses: actions/upload-artifact@3fba4c40e4e76c389fa9b6c5a0a09e73c579cd4c

        if: always()
        with:
          name: Cypress Results
          path: |
            cypress/screenshots
            cypress/videos
            cypress/results
          if-no-files-found: ignore
